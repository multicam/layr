/**
 * Data Validation Schemas — re-exported from @layr/types/schemas.
 * Import directly from '@layr/types/schemas' for new code.
 */
export {
  SCHEMA_DESCRIPTIONS,
  MetadataSchema,
  ValueOperationSchema,
  PathOperationSchema,
  FunctionArgumentSchema,
  FormulaSchema,
  ActionModelSchema,
  EventModelSchema,
  MediaQuerySchema,
  CustomPropertySchema,
  StyleVariantSchema,
  NodeModelSchema,
  ComponentSchema,
  PageSchema,
  ShallowComponentSchema,
  ShallowPageSchema,
  validateComponent,
  validatePage,
  validateFormula,
  validateAction,
  validateNode,
} from '@layr/types/schemas';

export type { ValidationResult, MediaQuery } from '@layr/types/schemas';
