{
  "name": "layr",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun run --filter './packages/backend' dev",
    "build": "bun run --filter './packages/*' build",
    "test": "bun test"
  },
  "devDependencies": {
    "bun-types": "latest",
    "happy-dom": "^20.6.1",
    "typescript": "^5.3.0"
  }
}
