{
  "name": "layr",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun run --filter './packages/backend' dev",
    "build": "bun run --filter './packages/*' build",
    "test": "bun test"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "bun-types": "latest"
  }
}
